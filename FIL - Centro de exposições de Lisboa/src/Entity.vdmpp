class Entity
	instance variables
		protected id: nat1;
		protected name: seq of char;
		
	operations	
		public pure getId : () ==> nat1
		getId() == (return id);
		
		public pure getName : () ==> seq of char
		getName() == (return name);
		
end Entity